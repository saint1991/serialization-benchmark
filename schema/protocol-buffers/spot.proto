syntax = "proto3";
package protobuf;
option java_package = "com.github.saint1991.serialization.benchmark.protobuf";

// enum should be CamelCase and each member should be UNDERSCORE_SEPARATED_CAPITALS
enum SpotType {
    A = 0; // begin with 0 in enum
    S = 1;
}

message Spot {
    int32 id = 1;
    SpotType type = 2;
}
